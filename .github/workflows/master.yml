name: Master

on:
  push:
    branches:
      - master
jobs:
  Test:
    name: Test
    runs-on: macOS-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Build and test
        run: xcodebuild clean test -destination 'name=iPhone 11 Pro' -scheme 'Yoke'